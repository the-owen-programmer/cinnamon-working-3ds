Here is how to build,

FFMPEG IS REQUIRED

1. Copy your data.win to /romfs/cinnamon/data.win
2. run 'python3 preprocessor.py "./romfs/cinnamon/data.win"'
2.5. KEEP the data.win at /romfs/cinnamon/data.win (THIS IS REQUIRED)
3. preprocessing will take awhile, the libary is bad and the python script is early.
4. run 'make'
5. this should work in emulator and real hardware.
